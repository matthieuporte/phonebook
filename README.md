# phonebook

The app is hosted on [this address](https://phonebook42.fly.dev/) for now.

I learnt how to use expressjs with mongo db. I used a (very little) bit of regex too to make sure the inputs were corrects.
